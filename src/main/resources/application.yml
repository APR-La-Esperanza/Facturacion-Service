server:
  port: ${SERVER_PORT:8083}

spring:
  application:
    name: facturacion-service

  datasource:
    url: ${DB_URL:jdbc:postgresql://ep-holy-bar-ap2gyxmn-pooler.c-7.us-east-1.aws.neon.tech/apr_facturacion_db?sslmode=require&channel_binding=require}
    username: ${DB_USERNAME:neondb_owner}
    password: ${DB_PASSWORD:npg_je1UtX9MuRhl}

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URL:http://localhost:8761/eureka}
  instance:
    prefer-ip-address: true
