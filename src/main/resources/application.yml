server:
  port: 8083

spring:
  application:
    name: facturacion-service

  datasource:
    url: jdbc:postgresql://ep-holy-bar-ap2gyxmn-pooler.c-7.us-east-1.aws.neon.tech/apr_facturacion_db?sslmode=require&channel_binding=require
    username: neondb_owner
    password: npg_je1UtX9MuRhl

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    prefer-ip-address: true
