Facturacion-Service (puerto 8083, apr_facturacion_db)
Responsabilidad: Emisión y gestión de facturas. Calcula consumo M3 (lecturaActual - lecturaAnterior). Entidad: Factura { id, socioId, periodo, lecturaAnterior, lecturaActual, consumoM3, montoPesos, estado: EstadoFactura, fechaEmision, fechaVencimiento } Controller: FacturacionController — GET con filtros (socioId, estado, periodo), POST, PUT, DELETE Service: FacturacionService — listarTodas(), buscarPorSocioId(), buscarPorEstado(), buscarPorPeriodo(), guardar(), actualizar(), eliminar(), validarSocioEnSocioService() (privado) Repository: FacturaRepository — custom: findBySocioId(), findByEstado(), findByPeriodo(), findBySocioIdAndPeriodo() DTOs: FacturaDTO, FacturaResponseDTO Dependencias externas: Socio-Service → GET /socios/{id} Tabla BD: facturas
